# Ω‑TTU‑LLM‑50M – Générateur de texte Streamlit

Ce projet implémente un modèle de langage compact (50M paramètres) avec une interface Streamlit.

## Installation locale
```bash
pip install -r requirements.txt
streamlit run app.py
